name: Validate Benchmark Release

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema

      - name: Validate benchmark package
        run: python scripts/validate_release.py

      - name: Build benchmark archive
        run: bash scripts/package_release.sh

      - name: Ensure enterprise docs exist
        run: |
          test -f docs/release-policy.md
          test -f docs/provenance.md
